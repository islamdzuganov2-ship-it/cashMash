<?xml version="1.0" encoding="utf-8"?>
<!-- Наружу отдаётся ТОЛЬКО каталог журналов, и только тот файл, который
     человек выбрал сам. Секреты (ops/.env) и состояние лежат рядом, но
     в этот список не входят: FileProvider отдаёт лишь перечисленное. -->
<paths>
    <files-path name="logs" path="robot/data/logs/" />
</paths>
