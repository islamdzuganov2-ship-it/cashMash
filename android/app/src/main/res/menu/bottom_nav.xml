<?xml version="1.0" encoding="utf-8"?>
<!-- Четыре вкладки, разнесённые по РОДУ содержимого, а не по темам:
     состояние процесса, картина рынка, диагностика, настройка. Пятой
     быть не должно — на нижней панели больше четырёх подписей уже не
     читаются на узком экране. -->
<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <item
        android:id="@+id/tab_robot"
        android:icon="@drawable/ic_tab_robot"
        android:title="@string/tab_robot" />
    <item
        android:id="@+id/tab_panel"
        android:icon="@drawable/ic_tab_panel"
        android:title="@string/tab_panel" />
    <item
        android:id="@+id/tab_logs"
        android:icon="@drawable/ic_tab_logs"
        android:title="@string/tab_logs" />
    <item
        android:id="@+id/tab_settings"
        android:icon="@drawable/ic_tab_settings"
        android:title="@string/tab_settings" />
</menu>
